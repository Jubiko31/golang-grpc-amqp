version: '3'

services:
  broker-service:
    build:
      context: ./../broker
      dockerfile: ./../broker/broker.Dockerfile
    restart: always
    ports: 
      - "8080:8000"
    deploy:
      mode: replicated
      replicas: 1